# PR 审查报告：5 个统计与功能类 PR

> 审查日期：2026-07-26
> 审查人：资深代码审查员（AI Agent）
> 仓库：xiabai2008/quantum-rl-scheduler

---

## CI 状态总览

| PR | Lint | Test (3 versions) | TypeCheck | Benchmark | Mutation |
|:---|:---:|:---:|:---:|:---:|:---:|
| #277 | FAIL | SKIPPED | SKIPPED | SKIPPED | SKIPPED |
| #283 | PASS | PASS | **FAIL** | FAIL* | FAIL* |
| #275 | PASS | PASS | PASS | FAIL* | FAIL* |
| #279 | PASS | PASS | PASS | FAIL* | FAIL* |
| #284 | FAIL | SKIPPED | SKIPPED | SKIPPED | SKIPPED |

> *Benchmark 和 Mutation 失败为 main 分支预存问题（mutmut 3.x 兼容性 + benchmark 环境依赖），非 PR 引入。

---

## PR #277: feat(#211): 补充统计检验力分析（Power Analysis）与 MDES

**作者**：NN2914 | **分支**：feature/issue-211-power-analysis

### 1. 核心修改内容

修改文件 11 个，核心变更：

- **`src/utils/stats_significance.py`**：新增 3 个函数
  - `power_ttest(d, n1, n2, alpha)` — 基于非中心 t 分布计算事后检验力
  - `minimum_detectable_effect(n1, n2, alpha, power)` — 计算 MDES（最小可检测效应量）
  - `sample_size_for_effect(d, alpha, power, ratio)` — 迭代求解达到目标检验力所需样本量
- **`scripts/evaluation/statistical_significance.py`**：在报告中新增"五、检验力分析"章节，含表格和文字解读
- **`tests/test_stats_significance.py`**：新增 `TestPowerAnalysis` 测试类，16 个测试用例
- **`results/reports/power_analysis.md`**：新增检验力分析报告
- 其余文件为 ruff format 格式化（annealing.py、marl.py 等）

### 2. 统计方法正确性分析

**`power_ttest` 函数** — 正确：
- 使用非中心 t 分布（`scipy.stats.nct`）计算双侧检验力，公式为 `power = P(T > t_crit | ncp) + P(T < -t_crit | ncp)`，非中心参数 `ncp = |d| * sqrt(n1*n2/(n1+n2))` — 这是标准方法。
- 使用 survival function `stats.nct.sf(t_crit, df, ncp)` 计算右尾，比 `1 - cdf` 数值更稳定。
- 对左尾 `stats.nct.cdf(-t_crit, df, ncp)` 在 ncp 较大时可能返回 NaN（数值下溢）做了显式容错处理 — 这是良好的工程实践。
- 边界处理完善：n<2 返回 nan，d=NaN 返回 nan，结果裁剪到 [0, 1]。

**`minimum_detectable_effect` 函数** — 正确：
- 使用经典近似公式 `MDES = (t_alpha + t_beta) * sqrt((n1+n2)/(n1*n2))`，来自 Cohen (1988)，是广泛使用的标准公式。

**`sample_size_for_effect` 函数** — 正确但效率一般：
- 迭代搜索 n=2 到 100000，直到检验力达标。虽然可使用闭式公式，但迭代法更灵活（支持不等样本量比例），且搜索范围足够。

**统计问题 — 对非参数检验计算检验力**：
- 报告生成代码对**所有**比较对（包括 rank-biserial 相关性的非参数检验结果）都计算了检验力，注释写"对非参数检验用 |effect| 近似"。这在统计学上是**有争议的**——rank-biserial 与 Cohen's d 量纲不同，直接代入 t 检验检验力公式不严谨。报告虽有免责声明，但容易误导读者。

### 3. 代码正确性分析

- `power_ttest` 数值稳定性好（sf + nan 容错）
- 报告生成中使用 `data.get(n1_key, [])` 安全访问，避免 KeyError
- 测试覆盖全面：符号不变性、零效应、单调性、边界条件、目标检验力验证
- **Lint 失败原因**：ruff format 检查未通过，可能是格式化变更与 main 分支不一致

### 4. 项目价值评估

**高价值**。本项目核心卖点之一是统计严谨性（N=250, p=1.032e-42, Bonferroni 校正）。补充 Power Analysis 和 MDES 可以：
- 回答评委"样本量是否足够"的质疑
- 为真机小样本实验（N=5）提供检验力佐证
- 体现统计方法论的完整性

### 5. 潜在风险

- Lint 失败导致所有下游 CI Job 被跳过，无法验证测试和类型检查
- 非参数检验的检验力计算可能被评委质疑

### 6. 处理建议：**需修改**

需修复：
1. 修复 ruff format lint 失败
2. 检验力分析应仅对 Cohen's d 效应量计算，非参数检验标注"N/A（非参数检验）"

---

## PR #283: feat: add statistical power analysis and MDES to significance reports

**作者**：NN2914 | **分支**：feature/issue-211

### 1. 核心修改内容

修改文件 10 个，与 #277 高度重叠：

- **`src/utils/stats_significance.py`**：新增相同的 3 个函数（`power_ttest`、`minimum_detectable_effect`、`sample_size_for_effect`）
- **`scripts/evaluation/statistical_significance.py`**：新增检验力分析章节
- **`tests/test_stats_significance.py`**：新增 `TestPowerAnalysis` 测试类，8 个测试用例
- **`.github/workflows/ci.yml`**：mutation testing 增加 `continue-on-error: true`；benchmark job 增加 `requirements-dev.txt` 安装

### 2. 与 #277 的重复性分析

**两个 PR 高度重复**，都为 Issue #211 实现了相同的 3 个函数。关键差异如下：

| 维度 | PR #277 | PR #283 |
|:---|:---|:---|
| `power_ttest` 数值稳定性 | 使用 `sf()` + nan 容错 | 直接 `1 - cdf + cdf`，无 nan 处理 |
| 非参数检验检验力 | 计算（有争议） | **不计算**，标注"N/A（非参数检验）" |
| 测试用例数 | 16 个 | 8 个 |
| 报告安全性 | `data.get()` 防 KeyError | `data[]` 直接访问，有 KeyError 风险 |
| power_analysis.md 报告 | 有 | 无 |
| CI 改进 | 无 | 有（mutmut continue-on-error, requirements-dev.txt） |
| 真机小样本检测 | 无 | 有（N=5 时自动补充检验力说明） |
| mypy | 未验证（lint 挡住） | **失败** |

### 3. 统计方法正确性分析

**`power_ttest` 函数** — 正确但不够健壮：
- 同样使用非中心 t 分布，公式正确
- 但 `power = float(1.0 - stats.nct.cdf(t_crit, df, ncp) + stats.nct.cdf(-t_crit, df, ncp))` 中，当 ncp 较大时 `stats.nct.cdf(-t_crit, df, ncp)` 可能返回 NaN，导致整个结果为 NaN。#277 对此做了容错，#283 没有。

**检验力计算范围** — 比 #277 更正确：
- 仅对 `effect_type == "Cohen's d"` 的比较对计算检验力，非参数检验标注"N/A（非参数检验）"。这在统计学上**更严谨**。

**MDES 和 sample_size** — 与 #277 完全相同，正确。

### 4. 代码正确性分析

- `power_ttest` 缺少 nan 容错，大效应量时可能返回 NaN
- 报告生成中 `n1 = len(data[pair.split(" vs ")[0]])` 直接索引，如果 pair key 与 data key 不完全匹配会抛 KeyError
- mypy 失败（具体原因未能从 CI 日志中提取，可能是 `stats.nct.cdf` 返回类型或脚本中的类型问题）
- 测试较少（8 vs 16），缺少符号不变性、单调性等重要测试

### 5. 项目价值评估

与 #277 价值相同，但额外包含 CI 改进（mutmut continue-on-error 对项目有实际价值——当前 mutmut 在 CI 中始终失败）。

### 6. 潜在风险

- mypy 失败需修复
- `power_ttest` 大效应量时可能返回 NaN
- 报告生成可能 KeyError

### 7. 处理建议：**拒绝**（与 #277 合并后取长补短）

#283 和 #277 实现相同功能，不应同时合并。建议：
- 以 **#277 为基础**（数值更稳定、测试更全面、代码更安全）
- 从 #283 借鉴：仅对 Cohen's d 计算检验力的逻辑 + CI 改进 + 真机小样本检测
- 关闭 #283

---

## PR #275: feat: 新增 solver_type 属性，记录退火求解器类型 (Issue #226)

**作者**：qpqpalalzmzm112 | **分支**：feat/issue-226-solver-type-tracking

### 1. 核心修改内容

修改文件 7 个：

- **`src/quantum/annealing.py`**：
  - 新增公开属性 `self.solver_type: str = "none"`
  - `anneal()` 方法在所有退火路径中设置 `solver_type`（`real_quantum`/`neal_sa`/`numpy_sa`）
  - `_last_solver` 保持同步作为向后兼容别名
  - `_last_anneal_stats` 字典新增 `solver_type` 字段
  - **注意**：neal 路径的 `_last_solver` 从 `"neal"` 改为 `"neal_sa"`
- **`src/quantum/annealing_loop.py`**：退火历史记录新增 `solver_type` 字段，日志中增加求解器信息
- **`scripts/evaluation/annealing_solver_comparison.py`**：monkey-patch 处同步设置 `solver_type`
- **`tests/test_annealing.py`**：新增 8 个测试覆盖所有退火路径
- **`tests/test_annealing_loop.py`**：新增 `test_solver_type_in_history_records` 测试

### 2. 代码正确性分析

**整体正确**，但发现一个向后兼容性问题：

- `annealing.py` 第 383 行将 neal 路径的 `_last_solver` 从 `"neal"` 改为 `"neal_sa"`
- `scripts/evaluation/annealing_solver_comparison.py` 第 486 行有 `if solver_a == "neal":` 的比较逻辑
- PR **未更新**该比较，导致 neal 求解器在比较脚本中无法被正确识别
- 该 bug 未被测试覆盖（neal 测试在无 D-Wave SDK 时跳过，比较脚本可能无测试覆盖）

观测维度索引验证 — 正确：
- `solver_type` 公开属性设计合理，类型标注完整
- 与 `_last_solver` 同步机制正确
- `_last_anneal_stats` 和历史记录中新增字段不影响现有消费者（向后兼容）

### 3. 项目价值评估

**中等价值**。`solver_type` 属性提供了更好的退火诊断能力：
- 可以追踪每次退火实际使用的求解器（真机 vs neal vs numpy）
- 在退火闭环的历史记录中记录求解器类型，便于分析
- 对答辩中"退火到底用了什么求解器"的问题有辅助说明作用

但对于比赛核心结论（PPO +88.3%）没有直接影响。

### 4. 潜在风险

- **`"neal"` → `"neal_sa"` 重命名导致 `annealing_solver_comparison.py` 第 486 行逻辑断裂**——neal 求解器将无法被正确识别，可能导致比较实验结果错误
- 该 bug 仅在有 D-Wave neal SDK 的环境中触发，CI 中 neal 测试被跳过

### 5. 处理建议：**需修改**（小修复）

修复项：
1. 更新 `scripts/evaluation/annealing_solver_comparison.py` 第 486 行：`if solver_a == "neal":` → `if solver_a in ("neal", "neal_sa"):`
2. 或更优雅地使用 `solver_type` 属性替代 `_last_solver` 比较

修复后可合并。核心功能实现正确，测试覆盖充分。

---

## PR #279: fix: 在 QUANTUM_ACCELERATION_ENABLED 检查处增加降级日志 (Issue #229)

**作者**：qpqpalalzmzm112 | **分支**：fix/issue-229-degradation-log

### 1. 核心修改内容

修改文件 7 个，但**实际有效变更仅约 7 行**：

- **`src/quantum/annealing.py`**：在 `QUANTUM_ACCELERATION_ENABLED` 全局变量定义后，新增启动时日志：
  ```python
  if QUANTUM_ACCELERATION_ENABLED:
      logger.info("量子加速已启用 (QUANTUM_ACCELERATION_ENABLED=1)")
  else:
      logger.warning(
          "量子加速已禁用 (QUANTUM_ACCELERATION_ENABLED 未设置或非 1/true/yes)。"
          "系统将使用纯经典 RL 优化流程，退火加速功能不可用。"
          "如需启用，请设置环境变量 QUANTUM_ACCELERATION_ENABLED=1。"
      )
  ```
- 其余文件（annealing_lr_sweep.py、annealing_loop.py、marl.py、test_annealing.py、test_annealing_loop.py）均为 **CRLF→LF 行尾转换**导致的全文"变更"
- ci.yml 为行尾转换 + mutmut continue-on-error

### 2. 代码正确性分析

- 逻辑正确：在模块加载时检查开关状态并记录日志
- **行尾转换问题严重**：整个 annealing.py（2205 行）因 CRLF→LF 被标记为完全重写，diff 达 5258 行，严重污染代码审查
- 日志在模块导入时触发（`QUANTUM_ACCELERATION_ENABLED` 是模块级变量），意味着每次 `import src.quantum.annealing` 都会输出此警告——在测试环境中可能产生大量噪音

### 3. 项目价值评估

**低价值**。该修改仅添加一条启动日志，对项目功能无实质影响：
- 现有代码在 `optimize_policy` 和 `optimize_policy_hierarchical` 中已有 `if not QUANTUM_ACCELERATION_ENABLED: logger.warning(...)` 的检查
- 新增的模块级日志只是提前告知，但不改变任何行为
- 对比赛答辩没有直接帮助

### 4. 潜在风险

- CRLF→LF 行尾转换会影响所有后续 PR 的 diff 可读性
- 模块导入时输出 warning 可能在 pytest 中触发警告捕获问题
- 5258 行 diff 中混入大量无关变更，增加审查负担

### 5. 处理建议：**需修改**

修复项：
1. 移除 CRLF→LF 行尾转换，仅保留实际功能变更（7 行）
2. 考虑将日志级别从 `warning` 降为 `info`，或添加静默机制避免测试噪音
3. 评估是否真的需要此日志——现有 `optimize_policy` 入口已有降级警告

修复后可合并，但优先级最低。

---

## PR #284: feat(#230-#235): 基线环境一致性+奖励验证+RL因果链

**作者**：qpqpalalzmzm112 | **分支**：fix/issue-230-235-baseline-env-consistency

### 1. 核心修改内容

修改文件 14 个，涉及 5 个 Issue，是本批 PR 中变更最大的：

- **Issue #230 — EnvBasedScheduler**（`src/scheduler/baselines.py`）：
  - 新增 `EnvBasedScheduler` 基类和 4 个子类（FCFS、SPTF、EDF、Greedy）
  - 将经典调度策略封装为 Gymnasium 环境动作选择器（输出动作 0/1/2）
  - 新增 `get_all_env_based_schedulers()` 工厂函数
- **Issue #231 — run_baseline_comparison use_env 模式**（`src/scheduler/baselines.py`）：
  - `run_baseline_comparison` 新增 `use_env`、`env_config`、`seed` 参数
  - `use_env=True` 时在 `QuantumSchedulingEnv` 中运行基线，reward 由 `env.step()` 返回
  - `use_env=False`（默认）保持向后兼容
- **Issue #233 — 奖励一致性验证**（`tests/test_baselines.py`）：
  - `TestBaselineRewardConsistency` 类验证 env_based 模式的 reward 来自 `env_reward.py`，非独立公式
  - 验证 legacy 模式和 env_based 模式 reward 不同
- **Issue #234 — RL 动作上下文**（`src/scheduler/env.py`、`env_real_machine.py`）：
  - `submit_to_real_machine` 新增 `rl_action`、`rl_action_prob`、`observation_snapshot` 参数
  - pending 记录中增加 RL 动作上下文字段，建立"RL 决策→真机任务"因果链
  - `env.py` 的 `step()` 方法构建观测快照并传递给路由方法
- **Issue #235 — 因果反馈记录**（`src/scheduler/env_real_machine.py`）：
  - 新增 `_record_causal_feedback()` 函数，记录完整的"RL 动作→真机任务→结果→reward"因果链
  - 在成功、失败、超时场景下均写入记录
  - `env.py` 新增 `_real_feedback_log` 列表属性

### 2. 代码正确性分析

**观测维度索引** — 验证正确：
- `EnvBasedFCFSScheduler` 使用 `observation[8]` (OBS_TASK_TYPE_QUANTUM)、`observation[9]` (OBS_TASK_TYPE_CLASSICAL)、`observation[0]` (OBS_QUBIT_AVAILABILITY) — 与 `env_types.py` 定义一致
- `EnvBasedSPTFScheduler` 额外使用 `observation[3]` (OBS_FIDELITY) — 正确
- `EnvBasedEDFScheduler` 额外使用 `observation[7]` (OBS_URGENCY_LEVEL) — 正确

**策略映射逻辑** — 合理：
- FCFS：量子任务+资源充足→量子，经典任务→经典，无任务→经典空操作
- SPTF：量子+高保真→量子（等效时间最短），量子+低保真→混合
- EDF：紧急+量子可用→量子加速，量子+资源紧张→混合
- Greedy：量子+高保真→量子（最高奖励），中保真→混合（避免打折）

**奖励一致性验证** — 正确：
- classical reward = REWARD_CLASSICAL(5.0) + REWARD_SUCCESS_BONUS(3.0) = 8.0
- legacy 独立公式 reward = 10.0 + priority*2.0 - wait*0.1，范围 [12, 20]
- 测试通过检查 reward 不在 [12, 20] 范围内来验证使用的是环境 reward — 逻辑正确

**发现问题**：
1. `avg_wait_time` 在 env_based 模式中被硬编码为 `0.0`——环境内部有等待时间数据但未暴露到结果中，这是一个功能缺失
2. `rl_action_prob` 在 `env.py` 的 `step()` 中始终传递默认值 `0.0`——`_route_to_machine` 调用时未传入动作概率，因果链中缺少概率信息。要获取动作概率需要访问 PPO 模型的动作分布，在 env 层面不可得，这是设计局限
3. **Lint 失败**：ruff format 检查在 `scripts/ci/audit_authoritative_metrics.py` 上失败——该文件**未被 PR 修改**，说明分支已过期，需要 rebase 到最新 main

### 3. 项目价值评估

**高价值**。这是本批 PR 中对项目科学性贡献最大的：

- **基线公平性**（#230/#231）：当前基线对比使用独立模拟流程和独立奖励公式，与 PPO 运行的 Gymnasium 环境奖励不一致。`EnvBasedScheduler` 使基线在相同环境中运行、使用相同奖励函数，消除了"基线 reward 与 PPO reward 不可比"的方法论缺陷。这对答辩中"PPO +88.3% 是否公平"的质疑至关重要。
- **因果链可追溯性**（#234/#235）：记录"RL 动作→真机任务→结果→reward"完整链路，对真机实验的可解释性和答辩可信度有显著提升。
- **测试设计**（#233）：奖励一致性验证测试设计巧妙，通过 reward 值范围区分环境奖励和独立公式奖励。

### 4. 潜在风险

- Lint 失败（分支过期）导致所有下游 CI Job 被跳过，无法验证测试和类型检查
- `avg_wait_time` 硬编码为 0.0，env_based 模式的对比结果缺少重要调度指标
- `rl_action_prob` 始终为 0.0，因果链不完整
- 变更面大（14 文件、5 Issue），合并冲突风险高

### 5. 处理建议：**需修改**

修复项：
1. **Rebase 到最新 main** 修复 lint 失败
2. 在 env_based 模式中从环境获取真实的 `avg_wait_time`（可从 `env._task_queue` 的 `wait_steps` 计算）
3. 在文档中注明 `rl_action_prob` 当前为占位值，需后续从 PPO 模型获取
4. 确保 mypy 和测试通过后合并

优先级最高的 PR，建议优先修复合并。

---

## 综合建议

### 优先级排序

| 优先级 | PR | 建议 | 理由 |
|:---:|:---|:---|:---|
| 1 | #284 | 需修改（rebase + 修复 avg_wait_time） | 科学价值最高，解决基线公平性核心问题 |
| 2 | #277 | 需修改（修复 lint + 非参数检验力） | 统计严谨性，高答辩价值 |
| 3 | #275 | 需修改（修复 neal 比较逻辑） | 小修复，诊断价值 |
| 4 | #279 | 需修改（移除行尾噪音） | 低价值，但修改量小 |
| 5 | #283 | 拒绝（与 #277 合并取长补短） | 重复实现，应合并到 #277 |

### #277 与 #283 合并方案

以 #277 为基础，融入 #283 的以下优点：
1. 检验力计算仅对 Cohen's d 生效，非参数检验标注"N/A"
2. CI 改进（mutmut continue-on-error + requirements-dev.txt）
3. 真机小样本（N=5）检验力自动说明

### 共性注意事项

- 所有 PR 的 Benchmark 和 Mutation CI 失败为预存问题，不阻塞合并
- 建议在合并前要求作者 rebase 到最新 main，避免 lint 失败和合并冲突
- PR #275、#279、#284 的作者 qpqpalalzmzm112 的 PR 普遍包含 ruff format 全文重格式化（可能是编辑器自动 CRLF→LF），建议在团队规范中统一行尾设置
