# === 核心依赖 ===
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# === 强化学习 ===
gymnasium>=0.28.0
stable-baselines3>=2.0.0
sb3-contrib>=2.0.0
torch>=2.0.0
tensorboard>=2.14.0

# === 量子计算 ===
# 仅保留量子退火所需依赖（dimod + dwave-neal）
# qiskit/pennylane 未在代码中使用，已移除以减小镜像体积
dimod>=0.12.0
dwave-neal>=0.6.0
# 注意：cqlib（天衍云 SDK）需从天衍云平台内部源获取，参见 requirements-quantum.txt

# === Web 框架 ===
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# === 数据可视化 ===
# 仅保留 matplotlib（报告生成用）；plotly/seaborn 未使用，已移除
matplotlib>=3.7.0

# === HTTP 客户端 ===
requests>=2.34.2
httpx>=0.25.0

# === 配置与工具 ===
python-dotenv>=1.0.0
pyyaml>=6.0.0
types-PyYAML>=6.0.12
loguru>=0.7.0
prometheus_client>=0.19.0
click>=8.1.0
tqdm>=4.66.0

# === Jupyter（已移至 requirements-dev.txt） ===
