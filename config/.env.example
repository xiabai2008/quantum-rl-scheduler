# 此文件已废弃（Issue #206）
#
# 项目唯一的环境变量模板位于仓库根目录的 .env.example。
# 请使用：
#   cp .env.example .env
#
# 历史上 config/.env.example 与根目录 .env.example 内容不一致，导致
# 12 个环境变量缺失声明、Mock 延迟默认值相差 90 倍等问题。
# 自 2026-07-26 起统一为根目录 .env.example，本文件保留仅为兼容旧文档引用。
#
# 详细环境变量说明请参阅根目录 .env.example 与 docs/api_reference.md。
