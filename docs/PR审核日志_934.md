# PR #934 审查记录（2026-08-06）

**标题**: feat: 量子信息TTL缓存 + 远程调用去重，提升调度可靠性
**作者**: qpqpalalzmzm112 | **状态**: ⛔ 需修改（5 项安全回归）

## 审查结论
**不能合并**。PR 核心价值（backend 信息 TTL 缓存 + 远程调用去重）方向合理，但实现显式删除了 main 上已合并的 **5 项安全/可靠性防护**，属于严重回归。作者需恢复防护后再合并。

## 五项安全/可靠性回退（合并验证）
| # | 防护 | 引入 Issue | main 状态 | PR 后状态 |
|:--|:--|:--|:--|:--|
| 1 | QCIS 电路内容验证 `_validate_qcis` | #515 | ✅ 存在 | ❌ 删除 |
| 2 | login_key 私有化 + 脱敏 repr | #735 | ✅ `_login_key` + `login_key=***` | ❌ 改回公开 `login_key` |
| 3 | platform 懒加载线程锁 | #701 | ✅ `_platform_lock` | ❌ 删除 |
| 4 | `_call_with_retry` 熔断器调用 | #719 等 | ✅ 46 处 | ⚠️ 删 before/on_success/on_failure/release_trial（28 处） |
| 5 | 断路器测试 5 个 | — | ✅ | ❌ 删除（test_api.py -118 行） |

## 其他问题
- `config/config.yaml` (+108/-106)：删除全部参数注释（取值范围/降级说明/Issue 映射）——工程文档损失
- `tests/test_api.py` -118 行：净删测试（熔断器 5 个）
- `tests/test_tianyan_sdk.py` -55 行：需确认删了什么

## 修改要求（给作者）
1. 恢复 `_validate_qcis` + 常量（#515）
2. 恢复 `_login_key` 私有 + 脱敏 `__repr__`（#735）
3. 恢复 `_platform_lock`（#701）——TTL 缓存也需加锁保护
4. 恢复 `_call_with_retry` 中的熔断器调用（#719）
5. 恢复 5 个断路器测试
6. 保留 config.yaml 参数注释（如需精简可另开 PR）
7. TTL 缓存实现保留（核心价值），但需线程安全

## 预判回归风险
若直接合并：提交超深/非法 QCIS 电路（真机费用损失）、API key 泄露（repr/序列化）、并发懒加载竞态、熔断失效（远程故障风暴）。高风险，禁止直接合并。
