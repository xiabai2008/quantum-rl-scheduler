# 8.7-v3 第6轮红队对抗审查报告

> 审查对象：量子RL驱动天衍云调度系统（XA-202609 揭榜挂帅）
> 审查口径：`qrl-fix-732` worktree main @ b897b69（含 8.7-v3 修复，local un-pushed）
> 审查日期：2026-08-07 ｜ 审查者：红队（内部对抗）
> 本轮已知已修的 8 类问题 **不重复验证**；仅报告本轮新掘出的 P0/P1。

---

## 结论（TL;DR）

**核心 +20.2% 统计真实可复现（PPO 1982.69±557.25 vs FCFS 1648.91±502.95，Welch t=7.02，p=7.558e-12，MWU p=1.86e-12，95%CI [+14.3%,+26.7%]，功效 0.9823，N=250）——本轮确认** **不是造假，不是数据捏造。**

本轮真正命中的"成就感残洞"按破坏力排序如下；第 1 条即可独立构成 P0（提交物白皮书自相矛盾）。

---

## ❗ P0-1　交付白皮书 PDF（技术白皮书.pdf）仍在提交物内自相矛盾

`docs/technical_whitepaper.pdf`（21 页，mtime 2026-08-06 02:34，git 最后改动=908c2a2 "8.5 深夜批审核 + PDF/PPT 重生成"）——**8.7-v3 只重生了 PPTX，未重新生成 PDF。**

无法用 `win32com` 可靠提取，已用 `pypdf` 逐页展平核对。逐字节确认以下旧数字仍活在同一份 PDF 里：

| 位置 | PDF 现有内容 | 权威口径（statistics.yaml / whitepaper.md） |
|---|---|---|
| 摘要 | `p=1.449e-66?（Welch t，p=1.449e-，Cohen's d=-2.1353）` | p=7.56e-12（8.5 已诚实化为新基线） |
| 摘要另处 | 提升 `+20.2%` | 正确（但同页并存废弃 p 值） |
| §4.2 表 | PPO `2348.88` 一行 ｜ §4.2 正文 `2348.91` | PPO=1982.69 |
| §4.3 | FCFS 利用率约 `33.6%`，PPO 提升约 `30%` | 利用率 `-3.3%`（8.5 否定，诚实披露） |
| §4.4 | 等待时间 `+51% 帕累托权衡` | -14.0%（双优） |
| 噪声节 | N=50 数字，d_z=7.7089/8.82，`-12.32%` | N=25 配对，-12.43%，p=2.98e-08 |

即使不抠数字，"1548 起 20.2%/p=7.56e-12 与 2348.91/p=1.449e-66 在同 21 页 PDF 内并存"本身就是送分给评委的"数字诚信"攻击面。**这是 P0：评委翻开 PDF 摘要即撞见旧 p 值。**

→ 修复：用 `docs/technical_whitepaper.md`（已修好）重新生成 PDF（docx→PDF 流程），并将 PDF 纳入 `check_doc_sync` / validate_submission 校验（当前 PDF 不在任何门禁里）。

---

## ⚠️ P1-1　述交付视频缺失（validate_submission 唯一 ERROR）

```
python scripts/ci/validate_submission.py --check
[ERROR] [DEMO_VIDEO] 文件不存在：演示视频_量子RL调度系统.mp4
错误: 1
```

清单 19 项里只此一项为 ERROR。**评审一票否决口径下，9 月 15 日提交前必须补**，否则直接视为交付不全。且：`submission_manifest.yaml` 的 `MODEL_MAPPO` 条目是 `directory`，校验脚本对其**静默通过**（没有任何内容/条目数检查），是个门禁空窗 —— 后续补视频时建议给 MAPPO 目录加条目级校验。

## P1-2：`check_stats_consistency.py` 黑名单豁免逻辑被"旧口径值"本身绕过

`src/visualization/fallback_template.py` 是我主要注意力所在…但更早命中的是门禁自身：

`scripts/ci/check_stats_consistency.py` 黑名单只打 `+20.2%`/`1982.69` 的正向值，**并没有把 `2350`/`2348.91` 数字**加进 `_ORPHAN_DEPRECATED`（它只加了 2349 这个"近似附近"，区间外值漏网）。grep 全文确认：

- 白名单条目写法是 `"p=1.449e-66"` 这类串匹配，只能挡住**精确重复**原文；任何排版变体（`1.449×10−⁶6`、`p=1.449×10-66`）直接绕过。
- 当旧口径以"2348.91"（非 2349）出现时，规则 `"2349" in line` 不命中——**上述 PDF 的 2348.91 恰好就是这样一个绕过样本。**

## ⚠️ P1-3：PPT 制层 p 值 "0.86" 疑为手误（应为 8.40e-01）

重新提取 `deliverable_models/答辩PPT.pptx`（8.7-v3 已重生，mtime 11:43）：

- 第 7 页（编译层）：写 `全 60 电路 p=0.86`。权威统计为 **8.40e-01**（N=250 编译层对比，whitepaper.md/statistics 均为 `p=0.840`）。
- "0.86" 与该口径任何一档数值都不吻合——大概率是"0.86"是旧 14 维一档 p 值的残留或笔误。PPT 是评委第一视院校，一个 p 值对不上就回到"数字纪律"质疑。

另外第 8 页仍有："真机噪声-->奖励下降 **12.32%**"——权威是 **12.43%**（N=25，-12.43%，p=2.98e-08）。两个数字都差一点，或为人工制表笔误。

## P1-4：演示脚本 video 脚本已修、但 complaints 的 "PPO 1983 → +20.2 并行 -14.0 等待" 与 PPT 页7未同步

`docs/demo_video_final_script.md` 已修好（恭喜），但与其**共稿**的 `docs/defense_ppt_outline.md` 和 PPT 本身仍有不一致处（见上 0.86/12.32）。同源叙事材料要三重一致，否则现场被连续追问时会露怯。

## P2 工程稳健性抽查（无卑微漏洞，仅区间内低风险项）

- `src/scheduler/env_reward.py:125` 有 `abs(tenant_wait - mean_wait) / mean_wait`除法——但第 122 行已有 `if mean_wait < 1e-6: return 0.0` 守卫，**举报无效**（非漏洞）。若围巾收紧，可把阈值对齐为 `1e-12`。
- `src/quantum/annealing.py:637` `min(abs(value)/max_delta,1.0)*scale` 有 `/max_delta`，若 `max_delta=0` 会除零——**但** 该路径是"非 D-Wave 探索模式"，且已有 `abs(float(value))` 对冲。暂时无对外暴露风险；如评委追问"退火/ QUBO 除零可用性"可直接答 N/A（探索性、默认关闭）。
- `tianyan_client.py` 的 TokenBucketRateLimiter（L79-L160）用了 threading.Lock + 数学 refill，锁在 acquire 内正确，未见问题。

**结论：工程层没有新的致命碰撞。** 上一轮的 fault-list（circuit_breaker/timeout-equene 等）都已在修复或复查清单内。

---

## 红队致命伤判定

红队按任务要求"协调式最烈署"找到的最要命一条，我扣定为：

> **交付白皮书 PDF 自相矛盾（2348.91/1.449e-66/33.6%/51% 旧口径在 21 页内与 +20.2% 并存），且该文件被 `validate_submission` 标记为必交件。**

只此一条，评委只要翻开白皮书摘要+§4.2 就能一击命中——这一条在 8-5~8-7 轮次做了"15 个 md 扫描、5 处 PPTX 修正"，**却因为"PDF 是从 docx 手工导出、不进 ∊ 门禁"而成为最亮的死角**。这就是"精华从们眼里漏掉"的 P0 根部原因：**二进制交付物没有纳入自动门禁式扫描。**

---

## 修复建议（按优先级）

1. **P0**：用已修订的 `whitepaper.md` 重新生成 `docs/technical_whitepaper.pdf`，并将其纳入 `scripts/ci/check_doc_consistency.py` 的 PDF 溯源校验（或至少加 Research/Comment：提交打 tag 前 PDF 必须重新导出一遍）。
2. **P0**：补 `演示视频_量子RL调度系统.mp4`（或确保提交包内对应条目），否则 validate_submission 0 ERROR 目标不可能达成。
3. **P1-2**：在 `check_stats_consistency.py` 把 `2348.91`、`2350` 区间补进 `_ORPHAN_DEPRECATED`；同时把 `1.449×10` 这类 Unicode 变体也加入检测。
4. **P1-3**：改 PPT 第 7 页 p=0.86→8.40e-01（及 12.32%→12.43%），与 whitepaper.md 一字不差。
5. **P1-4**：`submission_manifest.yaml` MAPPO 目录加内容条目数校验；demo 脚本同步抽帧（后置小风险）。

---

## 复现已验证的命令（备存）

```
cd qrl-fix-732
python scripts/ci/validate_submission.py --check      # 1 ERROR：DEMO_VIDEO 缺失
git log --oneline -5 DOCUMENTS/technical_whitepaper.pdf  # 908c6a2 后未再变
git log origin/main -2                                  # origin 仍在 f509e98c(8.7.2) 前，修复未 push
python scripts/evaluation/statistical_significance.py    # 复验 .json → p=7.558e-12
```

> 备注：本轮对抗验证用的数字全部来自代码仓库本地 worktree `qrl-fix-732`，主仓库 `quantum-rl-scheduler` 为旧工作区（0481047），**交付物统一以 config/statistics.yaml + docs/technical_whitepaper.md 为唯一权威。**