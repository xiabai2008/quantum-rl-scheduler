# 定时任务更新提示词（2026-07-23）

> 瑞哥发给各队友。每人找到自己的部分，复制代码块粘贴到 TRAE 发送即可。
> TRAE 会自动更新你已有的3个定时任务，不会创建新任务。
> 任务1「每日进度提醒」无需更新，只有任务2和任务3需要更新。
> 任务2从每周2次改为每日1次，任务3保持每周不变。

---

## DUMNOX

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "DUMNOX每周自动提Issue"（cron: 0 9 * * 1,4）
   - "DUMNOX每周代码自检"（cron: 0 15 * * 0）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：DUMNOX每周自动提Issue
=========================================

1. 将任务名称改为"DUMNOX每日自动提Issue"

2. cron表达式改为每日下午13:00执行：
   cron表达式：0 13 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：功能增强（强化量子AI双向赋能故事线的新功能，如新的量子退火应用场景、多目标调度策略扩展、DAG调度增强、多租户公平调度优化、混合调度器改进、异步退火闭环增强）
   备选方向1：性能优化（资源利用率提升、量子退火效率、重复加载/计算、可并行化操作）
   备选方向2：测试覆盖（覆盖率低于80%的模块、缺少边界/异常测试）
   不涉及方向：实验严谨性、落地价值、竞赛对齐、可解释性/可视化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：DUMNOX每周代码自检
=========================================

1. 所有pytest命令改为加并行参数：
   pytest tests/ -n auto --dist loadscope --tb=short -q

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

cron表达式和任务名称不变。
```

---

## heka-ky

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "heka-ky每周自动提Issue"（cron: 0 16 * * 1,4）
   - "heka-ky每周模块健康检查"（cron: 0 10 * * 6）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：heka-ky每周自动提Issue
=========================================

1. 将任务名称改为"heka-ky每日自动提Issue"

2. cron表达式改为每日下午13:30执行：
   cron表达式：30 13 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：落地价值（真实应用场景案例、ROI分析、VQE/量子化学/金融优化等场景适配、天衍云平台落地路径、价值量化指标完善、产业化可行性分析）
   备选方向1：可解释性/可视化（调度决策可解释性、监控面板增强、答辩演示效果优化、Web可视化改进）
   备选方向2：测试覆盖（覆盖率低于80%的模块、缺少边界/异常测试）
   不涉及方向：功能增强、实验严谨性、竞赛对齐、性能优化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：heka-ky每周模块健康检查
=========================================

1. 所有pytest命令改为加并行参数：
   pytest tests/ -n auto --dist loadscope --tb=short -q

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

3. Token失效处理改为：在Issue #58下评论"@heka-ky GH_TOKEN已过期，请重新执行认证命令"，不要创建新Issue。

cron表达式和任务名称不变。
```

---

## NN2914

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "NN2914每周自动提Issue"（cron: 0 9 * * 2,5）
   - "NN2914每周CI与覆盖率检查"（cron: 0 11 * * 6）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：NN2914每周自动提Issue
=========================================

1. 将任务名称改为"NN2914每日自动提Issue"

2. cron表达式改为每日下午14:00执行：
   cron表达式：0 14 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：实验严谨性（数据不一致检测、缺少统计检验/置信区间、实验可复现性、数据溯源链、消融实验补充、对照实验设计、效应量报告完善）
   备选方向1：竞赛对齐（多硬件兼容、多用户公平调度、瓶颈分析、天衍云平台适配）
   备选方向2：功能增强（强化双向赋能故事线的新功能）
   不涉及方向：落地价值、性能优化、可解释性/可视化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：NN2914每周CI与覆盖率检查
=========================================

1. 所有pytest命令改为加并行参数：
   pytest tests/ -n auto --dist loadscope --cov=src --cov-report=term-missing --tb=short -q

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

3. Token失效处理改为：在Issue #58下评论"@NN2914 GH_TOKEN已过期，请重新执行认证命令"，不要创建新Issue。

cron表达式和任务名称不变。
```

---

## Jackhock-1

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "Jackhock-1每周自动提Issue"（cron: 0 16 * * 2,5）
   - "Jackhock-1每周实验数据检查"（cron: 0 14 * * 6）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：Jackhock-1每周自动提Issue
=========================================

1. 将任务名称改为"Jackhock-1每日自动提Issue"

2. cron表达式改为每日下午14:30执行：
   cron表达式：30 14 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：竞赛对齐（多硬件兼容、多用户公平调度、瓶颈分析、天衍云平台适配、评审标准逐项对齐、提交清单完善、赛题要求覆盖度检查）
   备选方向1：实验严谨性（数据不一致、缺少统计检验/置信区间、对照实验设计）
   备选方向2：落地价值（真实应用场景、ROI分析、产业化路径）
   不涉及方向：功能增强、性能优化、可解释性/可视化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：Jackhock-1每周实验数据检查
=========================================

1. 新增多seed真机数据验证步骤（在原有50seed验证之后）：
   - 读取 results/real_machine/tianyan287_multiseed/ 目录下的真机实验数据
   - 验证多seed真机数据：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
   - 如果偏差超过5%，创建GitHub Issue报告数据不一致，分配给Jackhock-1

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

3. Token失效处理改为：在Issue #58下评论"@Jackhock-1 GH_TOKEN已过期，请重新执行认证命令"，不要创建新Issue。

cron表达式和任务名称不变。
```

---

## qpqpalalzmzm112

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "qpqpalalzmzm112每周自动提Issue"（cron: 0 9 * * 3,6）
   - "qpqpalalzmzm112每周代码自检"（cron: 0 15 * * 6）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：qpqpalalzmzm112每周自动提Issue
=========================================

1. 将任务名称改为"qpqpalalzmzm112每日自动提Issue"

2. cron表达式改为每日下午15:00执行：
   cron表达式：0 15 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：性能优化（资源利用率提升≥30%目标、量子退火效率优化、调度延迟降低、内存使用优化、重复加载/计算消除、可并行化操作、缓存策略改进）
   备选方向1：竞赛对齐（多硬件兼容、瓶颈分析、实验可复现）
   备选方向2：功能增强（强化双向赋能故事线的新功能）
   不涉及方向：实验严谨性、落地价值、可解释性/可视化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：qpqpalalzmzm112每周代码自检
=========================================

1. 所有pytest命令改为加并行参数：
   pytest tests/ -n auto --dist loadscope --tb=short -q

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

3. Token失效处理改为：在Issue #58下评论"@qpqpalalzmzm112 GH_TOKEN已过期，请重新执行认证命令"，不要创建新Issue。

cron表达式和任务名称不变。
```

---

## Izzro

> 复制以下代码块全部内容，粘贴到 TRAE 发送：

```
请更新我已有的定时任务，不要创建新任务。

步骤：
1. 列出我所有的定时任务，找到以下2个任务：
   - "Izzro每周自动提Issue"（cron: 0 16 * * 3,6）
   - "Izzro每周代码自检"（cron: 0 16 * * 6）
2. 逐个读取当前任务内容，然后按下面的修改点更新

=========================================
更新1：Izzro每周自动提Issue
=========================================

1. 将任务名称改为"Izzro每日自动提Issue"

2. cron表达式改为每日下午15:30执行：
   cron表达式：30 15 * * *
   时区：Asia/Shanghai

3. 将Issue发现方向改为以下优先级：
   主攻方向：可解释性/可视化（调度决策可解释性增强、监控面板Vue3/Echarts改进、Web可视化效果优化、答辩演示动画、Prometheus指标展示优化、实时调度过程可视化）
   备选方向1：落地价值（真实应用场景案例、价值量化指标、演示材料配套）
   备选方向2：竞赛对齐（落地价值文档、演示材料配套文档）
   不涉及方向：功能增强、实验严谨性、性能优化、文档完善（由其他队友负责，避免重复）
   注意：代码质量方向（ruff/mypy/docstring）已完成，不要再创建此类Issue
   优先从主攻方向找改进点，主攻方向无改进点时再从备选中选1个

4. 审视项目中"检查最近7小时创建的Issue"改为"检查最近1小时内创建的Issue避免与相邻时段队友重复"：
   gh issue list -R xiabai2008/quantum-rl-scheduler --state open --limit 10 --json number,title,createdAt

5. 在任务的约束条件部分，新增以下2条：
   - 检查我名下总open issue数（含所有标签），超过6个则不创建新Issue
   - 不活跃成员 zyhsga 和 K1660729 现阶段不参与项目，不要将Issue分配给他们
   - 宁缺毋滥，无改进点则不创建，不要为了凑数而创建低价值Issue

6. 在创建Issue的正文模板中，「## 关联」部分必须包含以下权威数字：
项目权威数字（不可篡改）：
- 50seed仿真：PPO=2746.94±1160.72 vs FCFS=1458.77±60.47, 提升+88.3%, Mann-Whitney U检验 p=1.032e-42, rank-biserial=-0.71
- 多seed真机：PPO=1665.22±324.51 vs FCFS=353.22±53.33, Cohen's d=5.33, p=6.83e-04（Bonferroni校正后显著）
- 所有实验数据和PR必须与以上数字一致

=========================================
更新2：Izzro每周代码自检
=========================================

1. 所有pytest命令改为加并行参数：
   pytest tests/ -n auto --dist loadscope --tb=short -q

2. 新增说明：已知flaky test `test_annealing_loop.py::test_callback_triggers_submit` 在Python 3.10/3.11上偶发失败（竞态条件，Issue #65已由PR#71修复）。如果仅此一个测试失败，不要创建Issue，属正常现象。

3. Token失效处理改为：在Issue #58下评论"@Izzro GH_TOKEN已过期，请重新执行认证命令"，不要创建新Issue。

cron表达式和任务名称不变。
```

---

## 每日提Issue 时间排布表

| 顺序 | 队友 | 执行时间 | cron表达式 | 主攻方向 |
|:--:|:--|:--|:--|:--|
| 1 | DUMNOX | 13:00 | `0 13 * * *` | 功能增强 |
| 2 | heka-ky | 13:30 | `30 13 * * *` | 落地价值 |
| 3 | NN2914 | 14:00 | `0 14 * * *` | 实验严谨性 |
| 4 | Jackhock-1 | 14:30 | `30 14 * * *` | 竞赛对齐 |
| 5 | qpqpalalzmzm112 | 15:00 | `0 15 * * *` | 性能优化 |
| 6 | Izzro | 15:30 | `30 15 * * *` | 可解释性/可视化 |

- 6人每人每天1次，相邻间隔30分钟
- 每次扫描前检查最近1小时内创建的Issue，避免与前面队友重复
- 与任务1「每日进度提醒」（09:30/10:30）不冲突
- 任务3「每周检查」保持每周不变

## Issue 方向分工表

| 队友 | 主攻方向 | 对齐评审标准 | 备选方向1 | 备选方向2 |
|:--|:--|:--|:--|:--|
| DUMNOX | 功能增强 | 技术创新性 | 性能优化 | 测试覆盖 |
| heka-ky | 落地价值 | 落地价值 | 可解释性/可视化 | 测试覆盖 |
| NN2914 | 实验严谨性 | 验证严谨性 | 竞赛对齐 | 功能增强 |
| Jackhock-1 | 竞赛对齐 | 主题契合度/方案可行性 | 实验严谨性 | 落地价值 |
| qpqpalalzmzm112 | 性能优化 | 方案可行性 | 竞赛对齐 | 功能增强 |
| Izzro | 可解释性/可视化 | 方案可行性/落地价值 | 落地价值 | 竞赛对齐 |

设计原则：
- 6个主攻方向全部对齐比赛评审标准（主题契合度/技术创新性/方案可行性/落地价值/验证严谨性）
- 代码质量方向已剔除（ruff 142→0、mypy 26→0、覆盖率91%，剩余改进为低价值）
- 测试覆盖和文档完善仅作为个别队友的备选方向，不做主攻
- 每人有1个主攻 + 2个备选，主攻无改进点时才动用备选
- 6个方向主攻不重叠，备选有交叉但保证灵活性
- 宁缺毋滥：无改进点则不创建，不要为了凑数而创建低价值Issue

## 更新内容对照表

| 队友 | 任务2更新点 | 任务3更新点 |
|:--|:--|:--|
| DUMNOX | +改为每日13:00 +方向分工 +总Issue上限6个 +排除不活跃成员 +权威数字 +宁缺毋滥 | +pytest并行参数 +flaky test标注 |
| heka-ky | +改为每日13:30 +同上 | 同上 +Token失效改到#58 |
| NN2914 | +改为每日14:00 +同上 | 同上 +Token失效改到#58 +覆盖率命令也加并行 |
| Jackhock-1 | +改为每日14:30 +同上 | 同上 +Token失效改到#58 +新增多seed真机数据验证 |
| qpqpalalzmzm112 | +改为每日15:00 +同上 | 同上 +Token失效改到#58 |
| Izzro | +改为每日15:30 +同上 | 同上 +Token失效改到#58 |

DUMNOX 任务3已有 Issue #58 引用，无需改 Token 处理。
Jackhock-1 任务3额外新增多seed真机数据验证。
NN2914 任务3含覆盖率检查，pytest命令也需加并行参数。
任务3保持每周执行不变，只改任务2为每日。
